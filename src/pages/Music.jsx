import React, {useEffect, useState} from 'react';
import {getById} from "../api/API";

const Music = () => {
    // const [music, setMusic] = useState(null);
    // useEffect(() => {
    //     getById("0").then(response => setMusic(response.data))
    // }, [])
    return (
        <div>

        </div>
    );
};

export default Music;